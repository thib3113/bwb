
# Instructions for Agents

1. **Dependency Management**: This project uses **pnpm**. Do not use `npm` or `yarn`.
   - Use `pnpm install` for dependencies.
   - Use `pnpm run <script>` for scripts.
   - Do NOT generate `package-lock.json`.
